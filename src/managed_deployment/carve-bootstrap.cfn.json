{
    "AWSTemplateFormatVersion": "2010-09-09",
    "Description": "Carve managed stack",
    "Parameters": {
        "OrgId": {
            "Type": "String",
            "Description": "AWS Organizations Id"
        },
        "Prefix": {
            "Type": "String",
            "Description": "Prefix carve AWS resources and stacknames with this"
        }
    },
    "Resources": {
        "SSMParameter": {
            "Type": "AWS::SSM::Parameter",
            "Properties": {
                "Name": {
                    "Fn::Sub": "/${Prefix}carve-stack-${AWS::StackName}"
                },
                "Description": "bootstrap carve API deployment",
                "Type": "String",
                "Value": {
                    "Fn::Sub": "${AWS::StackId}"
                }
            }
        }
    }
}